# CLAUDE.md

This file is a shim. The single source of truth for what Sigo is, how it works,
how to run it, and what may honestly be claimed about its token measurements is
the README.

**At the start of every session, read `README.md` in full before doing anything
else.** It covers the architecture (EN→ZH local translation → Claude → ZH→EN),
configuration and env vars, the benchmark methodology, and the
token-minimization mechanisms with their measured numbers and caveats.
